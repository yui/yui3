builddir=../../../builder/componentbuild

component=yui
component.rollup=true

# Path to sub-module build files
component.rollup.modules.buildfiles=yui-base.xml, get.xml, build-features.xml, build-rls.xml, build-intl-base.xml, \
                                    build-log.xml, build-later.xml, build-throttle.xml

# Rollup registration options
component.use=yui-base,get,features,rls,intl-base,yui-log,yui-later,yui-throttle
