
    // Progressively indent the text. This is purely visual for this example. 
    var marg = 0;
    Y.all('#demo li.one').each(function (v, k){
        v.setStyle('textIndent', (marg += 0.8) + 'em');
    });