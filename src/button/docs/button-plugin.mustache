<div class="intro">
    <p>In this example, we'll look at a few ways to create buttons using the `'button-plugin'` module, which combines `'button-core'`, `'attribute-core'`, and uses and `'cssbutton'` for styling.</p>
</div>