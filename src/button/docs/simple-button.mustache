<div class="intro">
    <p>In this example, we'll look at a few ways to create buttons using the `'button'` module, which includes `'button-base'`, `'button-group'`, and `'cssbutton'`.</p>
</div>

<div class="example">
    {{>simple-button-source}}
</div>

<h4>Source: HTML</h4>
```
{{>simple-button-source-html}}
```
<h4>Source: JavaScript</h4>
```
{{>simple-button-source-js}}
```
