<?xml version="1.0" encoding="UTF-8"?>
<project name="YuiBase" default="local">
    <description>Component Build File</description>

    <property environment="env" />

    <property file="build.properties" />
    <import file="${builddir}/3.x/bootstrap.xml" description="Default Build Properties and Targets" />

    <!-- TEMP HACK: Until state gets a build/finds a home -->
    <target name="register" description="Attribute State Combo" depends="YuiModuleTargets.register">
        <concat destfile="${component.builddir}/${component.basefilename}.js" append="true">
            <filelist dir="${srcdir}/util/yui/src/js" files="State.js" />
        </concat>
        <concat destfile="${component.builddir}/${component.basefilename}-debug.js" append="true">
            <filelist dir="${srcdir}/util/yui/src/js" files="State.js" />
        </concat>
    </target>
</project>
