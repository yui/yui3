Attributes can be configured to be <code>readOnly</code>, stopping them from being modified by the end user, or <code>writeOnce</code> allowing them to only be written once by the end user. This example demonstrates how to setup attributes as <code>readOnly</code> or <code>writeOnce</code> attributes, and shows how they differ.
