The <a href="attribute-basic.html">Basic Attribute</a> example shows how we can add attributes to a host class, and set up default values for them using the attribute configuration object. This examples explores how you can set up "set", "get" and "validator" functions for individual attributes, which can be used to modify or normalize attribute values during get and set invocations, and prevent invalid values from being stored. 


